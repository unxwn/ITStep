@model AirbnbPricePred.WebApp.Models.RentalResponse

@{
    ViewBag.Title = "Prediction result";
}

<div class="card">
    <div class="card-body">
        <h5 class="card-title">Predicted price</h5>
        <p class="card-text display-6">$@Model.PredictedPrice</p>
        <a href="/" class="btn btn-secondary">Back</a>
    </div>
</div>
<pre class="mt-3">@Model.RawResponse</pre>
